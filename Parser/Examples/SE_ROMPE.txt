A = &Y;
B = &X;
B = C;          // edge C -> B
if (cond) {
  A = B;        // edge B -> A + kill(A)
  B = A;       // edge A -> B + kill(B)
  B= &D;
}
B = C;          // edge C -> B  (otra vez)
C = &X;
